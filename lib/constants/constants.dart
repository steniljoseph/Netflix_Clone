import 'package:flutter/material.dart';

const backgroundColor = Colors.black;
const constWhiteColor = Colors.white;
const constButtonColorWhite = Colors.white;
const constBlackColor = Colors.black;
const backgroundGrey = Colors.grey;

const constWidht = SizedBox(
  width: 10,
);
const constHeight = SizedBox(
  height: 10,
);
